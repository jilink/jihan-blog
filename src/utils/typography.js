import Typography from "typography"
import typographyGateTheme from "typography-theme-fairy-gates"

const typography = new Typography(typographyGateTheme)
export const { scale, rhythm, otions } = typography
export default typography
